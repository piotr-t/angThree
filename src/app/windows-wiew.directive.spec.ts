import { WindowsWiewDirective } from './windows-wiew.directive';

describe('WindowsWiewDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowsWiewDirective();
    expect(directive).toBeTruthy();
  });
});
